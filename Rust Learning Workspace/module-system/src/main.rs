/*once we have a file that called main.rs in our src folder a binary 
crate will automatically created it's name is the same as the pkg name.
main.rs is the crate root. the crate root is the src file where the rust
compiler starts at whe building the crate. it also makes the root module
of the crate.
if we need more binary crates we will create a folder named bin and add them there*/
fn main() {
    
}
